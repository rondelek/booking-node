import styled from "styled-components";

const Wrapper = styled.div`
  h2 {
    color: black;
    font-size: 1rem;
    font-weight: 400;
    line-height: normal;
    opacity: 0.8;
  }

  h3 {
    color: black;
    font-size: 0.9rem;
    font-weight: 400;
    line-height: 1.7rem;
  }

  p {
    color: var(--gray);
    opacity: 0.5;
    /* padding-top: 5px; */
  }
`;

export default Wrapper;
